# نظام إدارة المخازن بالذكاء الاصطناعي

نظام متكامل لإدارة المخازن والمنتجات مدعوم بالذكاء الاصطناعي لتقديم تحليلات ذكية وتنبؤات تلقائية.

## 📋 نظرة عامة

نظام إدارة مخازن متقدم يوفر واجهة سهلة الاستخدام لإدارة المنتجات مع ميزات تحليلية ذكية تساعد في اتخاذ القرارات الاستراتيجية. النظام مبني باستخدام Flask ويدعم قاعدة بيانات SQLite.

## ✨ المميزات الرئيسية

### إدارة المنتجات
- ✅ إضافة وتعديل وحذف المنتجات
- ✅ رفع صور للمنتجات
- ✅ تصنيف المنتجات حسب الفئات
- ✅ تتبع الكميات والأسعار
- ✅ إدارة تواريخ الصلاحية
- ✅ البحث السريع في المنتجات

### المبيعات والمخزون
- ✅ تسجيل المبيعات
- ✅ تحديث المخزون تلقائياً
- ✅ تتبع المبيعات الإجمالية لكل منتج
- ✅ عرض المنتجات المتاحة للبيع

### الذكاء الاصطناعي والتحليلات
- 🤖 تحليل المخزون الذكي
- 📊 تحديد أكثر المنتجات مبيعاً
- ⚠️ تنبيهات المنتجات المنتهية الصلاحية
- 📉 تحذيرات المنتجات منخفضة المخزون
- 🔮 تنبؤات نفاد المخزون
- 💡 توصيات إعادة الطلب
- 📈 تحليل المبيعات والتنبؤات

### التقارير والإشعارات
- 📋 تقارير شاملة قابلة للتصدير (Excel/CSV)
- 🔔 إشعارات فورية للمنتجات المنتهية الصلاحية
- ⚡ تنبيهات للمنتجات منخفضة المخزون
- 📊 تقارير مخصصة حسب النوع

## 🛠️ التقنيات المستخدمة

- **Backend**: Python 3.x, Flask
- **Database**: SQLite
- **Data Analysis**: Pandas
- **Excel Export**: openpyxl
- **Frontend**: HTML, CSS, JavaScript
- **Icons**: Font Awesome

## 📦 التثبيت والإعداد

### المتطلبات الأساسية
- Python 3.7 أو أحدث
- pip (مدير حزم Python)

### خطوات التثبيت

1. **استنساخ المشروع أو تحميله**
   ```bash
   cd "store ai"
   ```

2. **تثبيت المكتبات المطلوبة**
   ```bash
   pip install -r requirements.txt
   ```

3. **تشغيل التطبيق**
   
   **الطريقة الأولى: استخدام ملف التشغيل السريع**
   ```bash
   start.bat
   ```
   
   **الطريقة الثانية: تشغيل يدوي**
   ```bash
   python backend.py
   ```

4. **الوصول إلى التطبيق**
   - افتح المتصفح وانتقل إلى: `http://localhost:5000`
   - للوصول من الشبكة المحلية: `http://0.0.0.0:5000`

## 🚀 الاستخدام

### الصفحة الرئيسية
تعرض إحصائيات عامة عن:
- إجمالي عدد المنتجات
- عدد المنتجات المنتهية الصلاحية
- عدد المنتجات منخفضة المخزون (≤5)

### إضافة منتج جديد
1. انتقل إلى صفحة "إضافة منتج"
2. املأ البيانات المطلوبة:
   - اسم المنتج
   - الفئة
   - الكمية
   - السعر
   - تاريخ الصلاحية (اختياري)
   - الوصف (اختياري)
   - صورة المنتج (اختياري)
3. اضغط "حفظ"

### عرض المنتجات
- تصفح جميع المنتجات مع إمكانية البحث
- تعديل أو حذف المنتجات
- عرض تفاصيل كل منتج

### تسجيل المبيعات
1. انتقل إلى صفحة "المبيعات"
2. اختر المنتج والكمية المباعة
3. يتم تحديث المخزون تلقائياً

### التحليلات الذكية
1. انتقل إلى صفحة "الذكاء الاصطناعي"
2. عرض التحليلات التلقائية:
   - أكثر المنتجات مبيعاً
   - المنتجات المنتهية الصلاحية
   - المنتجات منخفضة المخزون
   - توصيات إعادة الطلب
   - التنبؤات والإشعارات الذكية
3. تصدير التقارير إلى Excel

### المنتجات المنتهية الصلاحية
عرض قائمة بجميع المنتجات المنتهية الصلاحية مع إمكانية إدارتها.

## 📁 هيكل المشروع

```
store ai/
│
├── backend.py              # الملف الرئيسي للتطبيق (Flask)
├── requirements.txt        # المكتبات المطلوبة
├── start.bat              # ملف التشغيل السريع
├── store.db               # قاعدة البيانات (SQLite)
├── TODO.md                # قائمة المهام
│
├── templates/             # قوالب HTML
│   ├── base.html         # القالب الأساسي
│   ├── index.html        # الصفحة الرئيسية
│   ├── products.html     # صفحة المنتجات
│   ├── add_product.html  # إضافة/تعديل منتج
│   ├── sales.html        # صفحة المبيعات
│   ├── ai.html           # صفحة التحليلات الذكية
│   └── expired.html      # المنتجات المنتهية الصلاحية
│
└── static/               # الملفات الثابتة
    ├── style.css         # ملف التنسيق
    ├── script.js         # ملف JavaScript
    ├── LOGO.jpg          # شعار المشروع
    └── uploads/          # مجلد الصور المرفوعة
```

## 🔌 API Endpoints

### المنتجات
- `GET /products` - عرض جميع المنتجات
- `GET /add` - عرض نموذج إضافة منتج
- `POST /add` - إضافة منتج جديد
- `GET /edit/<id>` - تعديل منتج
- `POST /edit/<id>` - حفظ التعديلات

### API للمنتجات
- `GET /api/products` - الحصول على جميع المنتجات (JSON)
- `POST /api/products` - إضافة منتج جديد (JSON)
- `PUT /api/products/<id>` - تحديث منتج
- `DELETE /api/products/<id>` - حذف منتج

### المبيعات
- `GET /sales` - عرض صفحة المبيعات
- `POST /sales` - تسجيل مبيعات متعددة
- `POST /api/sale` - تسجيل مبيعة واحدة

### التحليلات الذكية
- `GET /ai` - صفحة التحليلات الذكية
- `GET /api/ai_report` - الحصول على تقرير التحليل (JSON)
- `GET /api/predictions` - الحصول على التنبؤات والإشعارات
- `GET /api/export_excel` - تصدير تقرير AI إلى Excel
- `POST /api/export_custom_excel` - تصدير تقرير مخصص

### المنتجات المنتهية
- `GET /expired` - عرض المنتجات المنتهية الصلاحية

## 🤖 ميزات الذكاء الاصطناعي

### التحليل التلقائي
- تحليل المخزون بشكل تلقائي
- تحديد أنماط المبيعات
- حساب معدل الاستهلاك
- تنبؤات نفاد المخزون

### التنبؤات الذكية
- **تنبؤ نفاد المخزون**: حساب الأيام المتوقعة لنفاد المخزون بناءً على معدل المبيعات
- **تنبؤات المبيعات**: تحديد المنتجات الشائعة التي تحتاج زيادة في المخزون
- **تنبؤات القيمة**: مراقبة المنتجات عالية القيمة

### الإشعارات الذكية
- إشعارات فورية للمنتجات المنتهية الصلاحية
- تحذيرات للمنتجات التي ستنتهي خلال 7 أيام
- تنبيهات للمنتجات منخفضة المخزون (≤2 وحدات)
- تحذيرات للمنتجات منخفضة المخزون (≤5 وحدات)

### التوصيات
- توصيات إعادة الطلب الفورية
- توصيات زيادة المخزون للمنتجات الشائعة
- توصيات مراجعة المنتجات قريبة الانتهاء

## 📊 التقارير

### أنواع التقارير المتاحة
1. **تقرير عام**: جميع المنتجات مع المعلومات الأساسية
2. **منتجات منتهية الصلاحية**: قائمة بالمنتجات المنتهية
3. **كمية منخفضة**: المنتجات التي تحتاج إعادة طلب
4. **أكثر مبيعاً**: ترتيب المنتجات حسب المبيعات
5. **تقرير شامل**: تقرير كامل مع إحصائيات

### تصدير التقارير
- تصدير إلى Excel (.xlsx)
- تصدير إلى CSV (في حالة عدم توفر openpyxl)
- تقارير مخصصة حسب النوع

## 🔒 الأمان

- التحقق من نوع الملفات المرفوعة
- تنظيف أسماء الملفات قبل الحفظ
- منع رفع الملفات الضارة
- استخدام SQLite مع معالجة آمنة للاستعلامات

## 🐛 حل المشاكل

### المشكلة: لا يمكن تثبيت المكتبات
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### المشكلة: خطأ في قاعدة البيانات
- احذف ملف `store.db` وسيتم إنشاؤه تلقائياً عند التشغيل

### المشكلة: البورت 5000 مستخدم
- عدّل السطر الأخير في `backend.py`:
  ```python
  app.run(host='0.0.0.0', port=5001, debug=True)
  ```

## 📝 التطوير المستقبلي

- [ ] إضافة نظام مستخدمين وصلاحيات
- [ ] دعم قاعدة بيانات PostgreSQL/MySQL
- [ ] إضافة API أكثر شمولاً
- [ ] دعم متعدد اللغات
- [ ] تطبيق جوال
- [ ] تكامل مع أنظمة الباركود
- [ ] تقارير متقدمة أكثر

## 📄 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه وتعديله بحرية.

## 👨‍💻 المساهمة

نرحب بجميع المساهمات! يرجى:
1. Fork المشروع
2. إنشاء فرع للميزة الجديدة
3. Commit التغييرات
4. Push إلى الفرع
5. فتح Pull Request

## 📞 الدعم

للدعم والأسئلة، يرجى فتح issue في المشروع.

---

**صُنع بـ ❤️ باستخدام Flask و Python**

